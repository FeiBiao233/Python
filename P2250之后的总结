这道题目做了很久 总结一些有关列表循环的知识
第一 range()函数只能正序
第二 python的数组要先开 比如ans=[0 for i in range(1,9)]
如果你想开在1~7，那么就要range(1,9)， 因为1~9循环8次，然后数组里面是0~8
还是一些老生常谈的问题：python的严格缩进和数据类型
输入进来的时候都是字符串
列表方便的转格式 num=[int(num[i]) for i in range(0,8)]

还有一个方便的小技巧 列表输出不要方括号
先把列表类型改成字符串 再用join 
ans=[str(ans[i]) for i in range(0,8)]
print(' '.join(ans[7:0:-1]))
真TM烦 不过学到了很多东西