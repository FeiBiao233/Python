print
如果想要输出在文件里 1.fp=open('D:/test.txt','a+') a+的意思是在原基础上面改动
                   2.print('sth',file=fp) 写fp只是为了方便，可以把上面fp的后面全部复制过来

输出不换行 引号之间用逗号 相当于空一格 用+号 不空格


利用反斜杠打出一些特殊字符
1.\' 相当于把引号归为字符串，系统不再识别
2.\n换行 \t四个字符一组 余出来的自动用空格补齐到4个 \r回行 只显示后面一行？ \b 退格
在字符串前面加r，使字符串内部的转义字符不起作用

不换行输出 print('nmsl',end=' ')
print() 默认输出一个回车